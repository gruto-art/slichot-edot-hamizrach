# מצב הפרויקט — 22 בספטמבר 2026

## סוף עונה (22/9/2026) — הכול כבוי, מה להדליק בשנה הבאה
עונת הסליחות תשפ״ז הסתיימה. כדי שלא ייווצרו חיובים:
- **המזין בפוד של גרוק בוט כבוי** ואינו יכול לעלות: `~/.feeder.env` שונה שמו
  ל-`~/.feeder.env.disabled-2026`. אין cron ואין systemd — הוא לא יחזור מעצמו.
- **מנעול עונה בקוד** (`KOTEL_ENABLED`, ברירת מחדל כבוי): אין קליטה, אין תמלול,
  אין חימום — גם אם נשארו מפתחות בסביבה. הדף מציג "המעקב החי מהכותל חוזר בימי
  הסליחות הבאים".
- **`.env` המקומי רוקן**; עותק ב-`.env.disabled-2026` (לא ב-git).
- **ElevenLabs: המפתח נמחק** בידי בעל האתר ב-22/9/2026. סגור.
- **TypeSafe (Jev): המפתח עדיין פעיל — לא נמצא היכן לבטלו.** נבדק ב-22/9:
  קריאה ל-`POST https://api.typesafe.ai/v1/systemone` עם המפתח מחזירה 422
  (כלומר עברה אימות), ובלעדיו 403. ניהול המפתחות נמצא ב-`https://console.typesafe.ai`
  (דורש התחברות עם החשבון שבו נוצר המפתח ב-18/9) — לחפש שם API Keys/Settings.
  עד אז ההגנה היא שהמפתח אינו בשום סביבה פעילה: רוקן מ-`.env` המקומי (עותק
  ב-`.env.disabled-2026`), הוזז עם קובץ הסביבה בפוד, ומנעול העונה חוסם את Jev
  בקוד. סיכון שיורי: מי שישיג את הקובץ יוכל לחייב את החשבון.
- **למחוק ב-Render dashboard**: `ELEVENLABS_API_KEY`, `OPENAI_API_KEY`,
  `TYPESAFE_API_KEY` (ה-CLI אינו מנהל משתני סביבה).

**להדלקה בשנה הבאה (אלול תשפ״ח):** `KOTEL_ENABLED=1` ב-Render וב-`~/.feeder.env`,
להחזיר את שם קובץ הסביבה בפוד, מפתח ElevenLabs חדש, ואז `~/run-feeder.sh`.

## ליל ערב יום כיפור (20/9) — מה עבד
המעקב רץ מקצה לקצה: 00:35–01:54, מ"אל מלך יושב" עד המילה האחרונה בקדיש
(5,356 מ-5,357). 294 מ-395 הקטעים הניבו מיקום (~75%), 0 כשלונות שליחה,
השהיה ~13–14 שנ׳ מתחילת הקטע. **20 הדקות הראשונות אבדו** לבדיקת הבוטים של
יוטיוב — מכאן החימום המקדים.

**קהל (נמחק מאז מהמסד, נשמר כאן):** 73 כניסות, 46 מבקרים, שיא 23:00–00:00.
55% הדליקו מעקב חי; חציון שהייה 28.6 דק׳. QR 56, וואטסאפ 4. מובייל 70/73.
**פרסומות:** דניאל 71 צפיות / 7 קליקים (9.9%), טכנולוגי 72 / 1 (1.4%),
שיתוף בוואטסאפ 4. הסתרות: 18 ו-15.

**חותמת הזמן של יוטיוב שקרית** (~3600 שנ׳ קבוע): השידור נסגר באותה דקה שבה
הקול שלנו הגיע לסופו — היינו בקצה החי. העיכוב האמיתי למשתמש ~13–14 שנ׳ שלנו
ועוד ההשהיה הרגילה של יוטיוב.

**אזהרה על המדידה:** `DATA_DIR=/var/data` אינו דיסק קבוע בתוכנית החינמית. כל
פריסה **וכל הפעלה מחדש** מאפסות את `analytics.db` — קרה ב-20/9 וב-22/9.
היסטוריה רציפה קיימת רק ב-GA4. לא נפתר.


## חי באוויר
- **אתר**: https://slichot.onrender.com
- **ריפו**: https://github.com/gruto-art/slichot-edot-hamizrach (ענף `main`)
- **Render**: שירות `srv-dal3cs2jnfac73cb8c90`, תוכנית free, פרנקפורט, Docker
- **לוח בקרה**: `/admin` · **סנכרון גבאי**: `/sync` (שניהם דורשים `ADMIN_TOKEN`)

> **נפרס ופעיל (18/9, 15:20)** — `12fc412` חי ב-Render, `/api/live/remote` מחזיר
> 401 בלי טוקן, והמזין במחשב של גרוק בוט רץ (האתר במצב `listening`). דחיפה: `GITHUB_TOKEN` בסביבה פג — לדחוף עם
> `env -u GITHUB_TOKEN git -c credential.helper='!gh auth git-credential' push`.

## מה הושלם
- סדר סליחות עדות המזרח מלא ומנוקד: 55 פרקים, 5,357 מילים (ממקור ספריא, תורת אמת)
  - הוסרו לבקשת בעל האתר: "קמתי באשמורת" ו"אנחנו בושנו"
- כל הטקסט מוטמע ב-HTML בצד השרת (SEO ללא תלות ב-JS) + JSON-LD + FAQ + OG
- מעקב חי מהכותל: קליטה → חיתוך → תמלול → התאמה → SSE
- מדידת כניסות first-party ב-SQLite + לוח בקרה
- **GA4** `G-MJHY3QN9G5` · **Search Console** אומת, מפת אתר הוגשה

## 18 בספטמבר — מזין מרוחק דרך המחשב של גרוק בוט (commits מקומיים)
יוטיוב חוסמת את Render. **הפתרון שנבחר: המחשב של גרוק בוט קולט ומתמלל, ושולח
לאתר רק מיקומים.** בעל האתר ויתר על קובץ העוגיות (אין חשבון גוגל חד-פעמי).

**נבדק ועובד** מהמחשב של גרוק בוט (Cloudflare, ‏`104.30.180.115`, טקסס):
yt-dlp מחלץ את שידור הכותל בלי חסימה, ו-20 שניות אודיו ירדו בהצלחה.
כתובת ה-IP יציבה, ולכן נעילת הקטעים ל-IP אינה מפריעה.

| commit | מה |
|---|---|
| `0487c05` | `POST /api/live/remote` (מוגן ב-`ADMIN_TOKEN`) + `scripts/kotel_feeder.mjs` |
| `9032a7b` | היגיון התקדמות (`server/tracker.js`), הטיית תמלול, נגינת קובץ בקצב אמיתי |

- **המזין** מריץ את אותו מנוע (`KotelEngine`) ושולח מיקומים לאתר. הוא בודק כל
  10 שניות כמה מאזינים יש באתר, **וקולט רק כשיש מאזינים** (תמלול עולה כסף).
  כל עוד הוא שולח פעימות, האתר אינו מנסה לקלוט בעצמו. אם השתתק — אחרי 75
  שניות האתר חוזר למצב "לא זמין". סנכרון גבאי (`/sync`) גובר על המזין.
- **היגיון התקדמות**: תנועה רק קדימה, עד 3.5 מילים לשנייה לפי הזמן שחלף.
  קפיצה (דילוג של החזן) מתקבלת רק כשהקטע הבא מאשר אותה **במילים חדשות בלבד**.
  התקדמות משוערת מוגבלת ל-30 מילים מעבר לזיהוי האחרון (`KOTEL_DRIFT_CAP`).
- **הטיית תמלול** `STT_BIAS=1`: שולח ל-ElevenLabs עד 90 צירופים (`keyterms`) של
  הטקסט הצפוי מהמיקום הנוכחי. עלות +20%. מעל 100 צירופים יש חיוב מינימום של
  20 שניות לבקשה — לכן 90.
- **לבדיקות**: `KOTEL_STREAM_URL=<קובץ מקומי>` מנוגן בקצב אמיתי;
  `KOTEL_START_SEC` נקודת התחלה.

### מדידות (קטעי 12 שניות, ElevenLabs)
| מה נמדד | לפני | אחרי |
|---|---|---|
| קפיצות הלוך-חזור (6 מקטעים, 3 הקלטות) | 3 | **0** |
| זיהוי, כותל 40:00–55:00 | 69% | **81%** (עם הטיה) |
| זיהוי, הרב אלבז 24:00–39:00 | 80% | **93%** (עם הטיה) |

ההטיה אינה "ממציאה": המיקומים עם הטיה ובלעדיה זהים (חציון הפרש 1–3 מילים).

**נבדק ונפסל:**
- `scribe_v2` — אינו טוב מ-`scribe_v1` (65% מול 69% על אותו קטע).
- **Whisper מקומי** (ivrit-ai large-v3-turbo, על המחשב של גרוק בוט) — מהיר מספיק
  (~4 שניות ל-12), אבל 36% זיהוי מול 81%, וממציא מילים בשירה. **הוסר לגמרי**
  מהקוד ומהמחשב של גרוק בוט לבקשת בעל האתר.
- OpenAI `gpt-4o-transcribe` — הקוד תומך (עם אותה הטיה ב-`prompt`), **לא נמדד**:
  אין מפתח.

## 18/9 — מקור השידור וקישור לבחינה (`946e850`)
- **המצלמה הקבועה (`LMHUcDktP-w`) אינה שידור הסליחות.** הערוץ (`UCvcdHbNAQvbe2GuIDLhlwaw`)
  מעלה לכל ערב שידור חי נפרד בשם "סליחות…". המנוע מחפש בלשונית `/streams` שידור
  `is_live` ששמו תואם `סליחות` (בודק שוב כל 3 דקות), ורק בהיעדרו נופל למצלמה.
- **לוח הבקרה `/admin`**: כרטיס "שידור למעקב החי" — הדבקת קישור (חי או הקלטה, `&t=`
  לנקודת התחלה) מעבירה את המזין אליו תוך ~10 שניות; "נקה" מחזיר לערוץ. הקלטה מנוגנת
  בקצב אמיתי. API: `GET/POST /api/live/source` (טוקן). הקישור נשמר ב-`DATA_DIR/live-source.json`.
- **נבדק בייצור**: הקלטת 17/9 מדקה 45 → המזין עבר, זיהה "אל מלך יושב" ו"ויעבור",
  והאתר התקדם. ניקוי → חזרה למצלמה.

- **חיסכון (`e0291ab`)**: בלי קישור מלוח הבקרה — קליטה רק בין **00:15 ל-02:00** שעון
  ישראל (`KOTEL_WINDOW`, לשנות ב-`~/.feeder.env` במחשב של גרוק בוט ואז `~/run-feeder.sh`),
  ורק משידור סליחות חי. המצלמה אינה מתומללת עוד (`KOTEL_CAMERA_FALLBACK=1` מחזיר).
  שידור שהתחיל בחלון נקלט עד סופו. האתר מציג את הסיבה כשהמעקב כבוי.

## 18/9 — ניסוי Jev (TypeSafe) לזיהוי הפרק
מפתח: `TYPESAFE_API_KEY` ב-`.env` המקומי. מודל `jev-latest`, `POST https://api.typesafe.ai/v1/systemone`.
סקריפטים: `research/jev_experiment.mjs` (שאלת Choice על 55 פרקים + "לא סליחות", לכל קטע 12 שנ׳
מהתמלולים השמורים) ו-`research/jev_filter.mjs` (שקלול HMM לאורך זמן). תשובות שמורות ב-`jev-*.json`.

| מדד (3 ערבי כותל, 1,012 קטעים) | תוצאה |
|---|---|
| התאמה לפרק של המנגנון הקיים (טקסט מלא, פרקים זהים = אותו פרק) | **80%** |
| כש-Jev בביטחון ≥0.8 | **90–97%** |
| קטעים שהמנגנון לא זיהה ו-Jev סימן "לא סליחות" | 160 מ-308 (כולל השידור המשותף 27:48–34:12 ב-0.97) |
| זמן בכל שנייה שבו יש פרק להציג | 85% מול 54% במנגנון הקיים (ברמת פרק, לא מילה) |
| עלות | ~23 אלף טוקנים לקריאה ≈ 0.001$; ≈0.30$ לשעת שידור |

- עם 60 מילים ראשונות בלבד לכל פרק: 61% גולמי → 82% עם המסנן. עם טקסט מלא: 80% גולמי, המסנן כמעט לא מוסיף.
- מצב "רציף" שמגביל לפרקים שאחרי הקודם — נכשל (ננעל על "אבינו מלכנו" מדקה 13 ולא יצא).
- חילוקי דעות: בגבולות פרקים המנגנון הקיים מהיר ומדויק יותר; במקומות אחרים Jev צודק
  (102:24 "אבינו הרחמן" — המנגנון אמר "קדיש"). **אין עדיין אמת-מידה ידנית.**
- ההמלצה: Jev כמפקח ליד המנגנון הקיים (עצירה ב"לא סליחות", התאוששות כשהמנגנון שותק,
  וטו על קפיצות) — לא כתחליף. מחר (ערב יוה"כ): להריץ במצב צל (רישום בלבד).
- **מצב צל פעיל (`82cbc3f`)** במזין במחשב של גרוק בוט (`JEV_SHADOW=1` ב-`~/.feeder.env`):
  לכל קטע נרשמים ב-`~/jev-shadow.jsonl` התמלול, החלטת המנגנון הקיים והפרק לדעת Jev. אינו
  משפיע על הדף. אחרי ערב אמיתי — להשוות, ולהחליט אם להפעיל עצירה/התאוששות/וטו.
- **השוואה לפי צליל** (ח=כ=ק, ט=ת, ס=ש, ע שקטה) נבדקה ב-`research/eval_tracker.mjs`:
  1,096→1,103 מ-1,871 קטעים — זניח, לא הוכנס.

## הצעדים הבאים (לפי סדר)
✅ צעדים 1–4 הושלמו ב-18/9. המפתחות ב-`~/.feeder.env` (הרשאות 600) במחשב של
גרוק בוט; הפעלה/הפעלה מחדש: `~/run-feeder.sh` (הורג מופע קודם), לוג: `~/feeder.log`.
אם המחשב של גרוק בוט אותחל — להריץ `~/run-feeder.sh` שוב.
0. ✅ Push נעשה. ✅ המחשב של גרוק בוט: `~/slichot` הוא clone טרי מ-GitHub
   (הישן נמחק, `full-kotel.m4a` הועבר לתוכו).
1. **פריסה** ל-Render (`git push`, ואז `render deploys create …`). ודא
   `curl -X POST …/api/live/remote` מחזיר 401 בלי טוקן.
2. ~~העתקת קוד~~ (נעשה) **למחשב של גרוק בוט** ל-`~/slichot` (כבר קיים שם עותק ישן עם קוד
   Whisper ב-`kotel.js` — להחליף). אין צורך ב-`npm install`: המזין משתמש רק
   ב-`server/kotel.js`, `matcher.js`, `tracker.js`, `data/*.json`.
3. **מפתחות למחשב של גרוק בוט**: `ELEVENLABS_API_KEY` ו-`ADMIN_TOKEN` של הייצור
   (נקראים ב-Render API; הקריאה נחסמה במצב האוטומטי).
4. **הפעלת המזין ברקע** במחשב של גרוק בוט:
   ```
   cd ~/slichot && nohup env FEED_TARGET=https://slichot.onrender.com \
     ADMIN_TOKEN=… STT_PROVIDER=elevenlabs ELEVENLABS_API_KEY=… STT_BIAS=1 \
     node scripts/kotel_feeder.mjs > ~/feeder.log 2>&1 &
   ```
   yt-dlp נמצא ב-`~/.local/bin` (גרסה 2026.08.19), ffmpeg במערכת.
5. **בדיקה מקצה לקצה** בזמן אמירת סליחות אמיתית בכותל.

## המחשב של גרוק בוט — חיבור
ראו את ה-skill `grok-bot-connect`. בקצרה: ממסר ב-`188.34.167.205:9911`,
העוזר `~/.claude/grokpod.sh 'פקודה'`. אם הוא לא עונה (`pod did not answer`),
בעל האתר מריץ **במסוף הלינוקס של הסוכן** (הדבקה אינה עובדת שם — לבקש מהסוכן
בצ'אט להריץ, או להקליד):
```
RELAY_URL=http://188.34.167.205:9911 RELAY_TOKEN=<מה-skill> python3 ~/p.py
```
**לא להריץ שני לקוחות במקביל.** פקודה ארוכה חוסמת את כל ה-bash שם — להריץ ברקע
(`setsid … &`) ולקרוא לוג.

## 19/9 — פרסומות, שיתוף בוואטסאפ, אבטחה
- **פרסומות בית** (`server/ads.js` — מקור אחד לבנייה ולשרת): באנר מונפש של 5 מסכים בלולאה (CSS, מסומן "פרסומת").
  עליון: כלים טכנולוגיים לעסקים → וואטסאפ 0527182810. תחתון: דניאל הצעות נישואין (לוגו `public/ads/danel-logo.png`
  מ-`~/Downloads/busines/proposal-6/danel-proposal-dreams`). וואטסאפ של דניאל: 0533104418 (`DANEL_WHATSAPP` גובר).
  קצב שונה: עליון 3 שנ׳ למסך, תחתון 4.5 (`frameSec`). כפתור × מופיע אחרי 30 שנ׳ ומסתיר את הבאנר ל-5 דקות
  (localStorage, נשמר גם ברענון; נספר כ-`ad_close`, עמודת "הסתרות" ב-`/admin`).
- **מעקב**: קליק = `GET /go/:id` (נרשם בשרת, `ad_click`, מפנה רק לכתובת מ-ads.js); צפייה = חצי באנר נראה שנייה
  (`ad_view`); שיתוף = `share_whatsapp`. טבלה "פרסומות ושיתופים" ב-`/admin`.
- **GA4**: `view_promotion` / `select_promotion` (promotion_id=tech|danel, creative_slot=top|bottom), `share`
  (method=whatsapp), `ad_close`. לוגו דניאל: `public/ads/danel-logo.webp` (חיתוך מהלוגו המלא, 253×120), alt ומידות קבועות.
- **כפתור שיתוף** בסרגל: `wa.me/?text=` עם קישור + `utm_source=whatsapp`.
- **אבטחה** (בדיקה ע"י agy): השוואת טוקן בזמן קבוע, טוקן רק בכותרת (לא ב-query), הגבלת קצב לאדמין (20 כישלונות/15 דק׳ ל-IP)
  ולנקודות המדידה, XSS בלוח הבקרה (escape), `/api/live/source` רק יוטיוב (SSRF), עד 6 חיבורי SSE ל-IP,
  HSTS/X-Frame-Options/Permissions-Policy. **לא טופל**: CSP (סיכון לשבור GA), מחיקת נתונים ישנים, `trust proxy=true`
  מאפשר זיוף X-Forwarded-For ועקיפת ההגבלה לפי IP.

## 19/9 — יומן מעקב חי ומדידת עיכוב
- **יומן** ב-`/admin` ("יומן מעקב חי", מתעדכן כל 5 שנ׳; API `GET /api/live/log?since=`): שורות מהאתר (חיבור/ניתוק
  מתפללים, מעבר מצב, מיקום שנשלח לכמה מתפללים, לוח בקרה, גבאי) ומהמזין (כל קטע: זמן תמלול, מספר מילים, החלטה
  step/jump/local או "לא זוהה" + הטקסט; כל מיקום: זמן שליחה ועיכוב). המזין שולח את שורותיו בפעימה. 800 אחרונות בזיכרון.
- **עיכוב נמדד** (הקלטת כותל, 19/9): תמלול 1.4–2.1 שנ׳, שליחה לאתר <20ms. מהמילה האחרונה בקטע עד הדף ~1.5 שנ׳;
  מהראשונה ~13.5 שנ׳ (אורך הקטע 12 שנ׳). **בנוסף** עיכוב יוטיוב עצמו — המזין מודד אותו כל דקה מ-`EXT-X-PROGRAM-DATE-TIME`
  ורושם ביומן (לא נמדד עדיין על שידור חי).

## 19/9 — קצב התקדמות לפי פרק
`research/pace_profile.mjs` מריץ את המנגנון על 5 הערבים השמורים (3 כותל, אלבז, חבושה — ערב יוה"כ), מודד קצב
לכל פרק (0.17 מילים/שנ׳ ב"שמע ישראל" עד 3.25 ב"לדוד אליך") ושומר `data/pace_profile.json` (‎`--write`).
ההתקדמות המשוערת בין זיהויים (`driftWpm` ב-`kotel.js`, רצה **באתר**) משתמשת בקצב הפרק; בלי מדידה — קצב
הזיהויים האחרונים. נבדק leave-one-out (הערב הנבדק אינו בפרופיל): נסיגות הדף 131→88, תזוזות מורגשות 215→175,
שגיאה ממוצעת 12.8→12.7 מילים. וריאנטים (ממוצע/מינימום) יצאו באמצע. כיבוי: `KOTEL_PACE_PROFILE=0` ב-Render.
תוקן גם: עיגול לכל שנייה השאיר את הדף במקום בפרקים איטיים — עכשיו שברי מילה מצטברים.
**לא נעשה:** שנים קודמות של ערב יוה"כ בכותל (צריך להוריד ולתמלל); חלון הקפיצה ב-`tracker.js` עדיין גלובלי (3.5 מ/ש).

## אחרי המזין: מערכת פרסומות
בעל האתר ביקש: פרסומות מתחלפות (כמו GIF) בשני המקומות הקיימים (`public/index.html`
שורות 34 ו-1876, כרגע placeholder), כניסת אדמין להעלאה וניהול, ומעקב צפיות
והקלקות לכל פרסומת בלוח `/admin`. הוא הזכיר "המפתח של אנבידיה" שיטפל בזה — לא
נמצא מפתח NVIDIA במחשב; לשאול אותו היכן, או לבנות כאן.

## לסשן הבא: סטטיסטיקת סליחות בכותל
בקשת בעל האתר: לתמלל כמה ערבי סליחות בכותל ולבנות סטטיסטיקה של **אורכי
פיוטים, תוספות (דרשות, מי שברך), עצירות, ונקודות שבהן הקהל שר**. המטרה: לשפר
את המעקב (למשל לדעת מראש מה מדלגים ואיפה עוצרים), ואולי להתאים את הנוסח.

**התמלולים כבר קיימים** ב-`research/kotel-transcripts/` (לא ב-git, `*.stt-*.json`).
פירוט המקורות ב-`sources.json`. להשוואת נוסח — רק הקבצים **בלי** סיומת מודל
(ללא הטיה). שלושה ערבים בכותל: 17/09 (מלא מ-07:00), 13/09, 09/09.
ההקלטות עצמן אינן שמורות; להוריד מחדש עם yt-dlp לפי מזהה היוטיוב. כדי
ש-`test_kotel` ישתמש במטמון בלי לשלם שוב, ההקלטה צריכה לשבת לידו באותו שם
(למשל `research/kotel-transcripts/kotel.m4a` ליד `kotel.m4a.stt-12s.json`):
```
yt-dlp -f 139/bestaudio -o research/kotel-transcripts/kotel.m4a 90gz7ziRG7g
```
yt-dlp במק היה ישן ונכשל ב-403 — עודכן ל-2026.08.19 (`python3.12 -m pip install -U yt-dlp`).

ממצאים ראשונים מערב 17/09 (עד דקה 77):
- 07:00–26:48 שירת "ידיד נפש" — לא סליחות.
- 27:00–35:24 שידור משותף עם כיכר החטופים בתל אביב, ברכות פתיחה.
- ~60:24 מי שברך לחיילי צה"ל באמצע הסליחות.
- החזן מדלג על בתים בפיוטים (למשל מ"רחמנא" ישר ל"ויעבור").
- 22 מ-55 הפרקים שלנו לא זוהו עד דקה 77 — חלקם כנראה אחרי (שומר ישראל, קדיש).

## עדיין פתוח מלפני
1. **בקשת אינדוקס לדף הבית** ב-Search Console — לא הושלמה.
2. **סטטוס מפת האתר** ב-Search Console — לוודא שכבר אינו "לא ניתן היה לאחזר".
3. **מפתח ElevenLabs הזמני** ב-`.env` המקומי — למחוק ב-ElevenLabs. שימש בסשן
   הזה לבדיקות ולתמלול ההקלטות (2,146 קטעים של 12 שניות, כ-7 שעות אודיו).
4. **Render לא מחובר ל-GitHub** — פריסות ידניות בלבד.
5. **זהות git**: שם המחשב השתנה ו-git אינו מזהה כתובת. ה-commits האחרונים נעשו
   עם `GIT_AUTHOR_*` כמו הקודמים. כדאי להגדיר `git config user.email`.

## תוצאות בדיקות המעקב (16 בספטמבר, לפני היגיון ההתקדמות)
| מקור | קטעים שעוגנו | התקדמות סבירה |
|---|---|---|
| הקלטת בית כנסת (נוסח מרוקאי) | 45/53 (85%) | 43/44 |
| הכותל המערבי (ד׳ בתשרי, הערוץ הרשמי) | 19/50 (38%) | 18/18 |

## פקודות
```bash
npm start                         # בנייה + שרת (טוען .env)
npm run test:kotel -- <קובץ> --start 2400 --seconds 900 --seg 12   # מעקב (tracker)
npm run test:kotel -- <קובץ> ... --no-tracker                      # ההתנהגות הישנה
STT_BIAS=1 npm run test:kotel -- <קובץ> ...                         # עם הטיה
render deploys create srv-dal3cs2jnfac73cb8c90 --output json --confirm
curl -s https://slichot.onrender.com/api/health | python3 -m json.tool
```
הבדיקה מדפיסה "קפיצות הלוך-חזור" — המדד למה שנראה בדף כקפיצות מצד לצד.
