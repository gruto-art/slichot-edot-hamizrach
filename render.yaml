services:
  - type: web
    name: slichot
    runtime: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: SITE_URL
        value: https://slichot.onrender.com
      - key: ADMIN_TOKEN
        generateValue: true
      - key: ANALYTICS_SALT
        generateValue: true
      - key: DATA_DIR
        value: /var/data
      # מעקב חי מהכותל — למלא כדי להפעיל
      - key: KOTEL_STREAM_URL
        value: https://www.youtube.com/watch?v=LMHUcDktP-w
      # יוטיוב חוסמת כתובות IP של מרכזי נתונים. קובץ העוגיות מועלה כ-Secret File
      # בשם cookies.txt דרך הדשבורד, ומוגש תחת /etc/secrets/.
      - key: YTDLP_COOKIES
        value: /etc/secrets/cookies.txt
      - key: STT_PROVIDER
        value: openai
      - key: OPENAI_API_KEY
        sync: false
      - key: ELEVENLABS_API_KEY
        sync: false
      # פרסומות (AdSense) — למלא לאחר אישור החשבון
      - key: ADSENSE_CLIENT
        sync: false
      - key: ADSENSE_SLOT_TOP
        sync: false
      - key: ADSENSE_SLOT_BOTTOM
        sync: false
      - key: GA_MEASUREMENT_ID
        sync: false
